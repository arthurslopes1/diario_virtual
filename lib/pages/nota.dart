import 'package:flutter/material.dart';
import 'package:diario_virtual/globals.dart' as globals;

class Nota extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(      
        child: Text("Tela de notas")
      )
    );
  }
}